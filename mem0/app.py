# -*- coding: utf-8 -*-
"""
基于 Mem0 和 Chainlit 的本地记忆聊天机器人
使用 Ollama 本地大模型和本地向量数据库
"""

import os  # 导入 os 模块，用于操作系统相关功能
import requests  # 导入 requests 库，用于发送 HTTP 请求
import chainlit as cl  # 导入 Chainlit 库，简写为 cl
from config import init_mem0, OLLAMA_BASE_URL, CHAT_MODEL  # 从 config 模块导入配置

# ========================================
# 第一步：代理绕过设置（在文件最开始）
# ========================================
# 确保 localhost 不走代理（已在 config.py 中设置，这里再次确认）
os.environ['NO_PROXY'] = 'localhost,127.0.0.1'
os.environ['no_proxy'] = 'localhost,127.0.0.1'

# ========================================
# 全局变量：Mem0 记忆实例
# ========================================
memory = None  # 初始化为 None，将在应用启动时创建


# ========================================
# 辅助函数：调用 Ollama 生成回复
# ========================================
def call_ollama(messages):
    """
    调用 Ollama API 生成聊天回复
    
    参数:
        messages (list): 消息列表，格式为 [{"role": "user", "content": "..."}, ...]
    
    返回:
        str: Ollama 生成的回复文本
    """
    # 构建 Ollama API 的完整 URL（/api/chat 端点）
    url = f"{OLLAMA_BASE_URL}/api/chat"
    
    # 构建请求体（JSON 格式）
    payload = {
        "model": CHAT_MODEL,  # 使用的模型名称
        "messages": messages,  # 传入的消息列表
        "stream": False  # 不使用流式输出，一次性返回完整回复
    }
    
    try:
        # 发送 POST 请求到 Ollama
        response = requests.post(url, json=payload, timeout=120)  # 设置 120 秒超时
        response.raise_for_status()  # 如果响应状态码不是 2xx，则抛出异常
        
        # 解析 JSON 响应
        result = response.json()
        
        # 提取并返回回复内容
        return result['message']['content']
    
    except requests.exceptions.RequestException as e:
        # 如果请求失败，返回错误信息
        return f"❌ Ollama 调用失败: {str(e)}"


# ========================================
# Chainlit 事件：会话开始
# ========================================
@cl.on_chat_start
async def on_chat_start():
    """
    当用户开始新的聊天会话时触发此函数
    用于初始化 Mem0 和发送欢迎消息
    """
    global memory  # 声明使用全局变量 memory
    
    # 初始化 Mem0（如果尚未初始化）
    if memory is None:
        try:
            memory = init_mem0()  # 调用 config.py 中的初始化函数
            print("✅ Mem0 已初始化")
        except Exception as e:
            # 如果初始化失败，打印错误并发送警告消息
            print(f"❌ Mem0 初始化失败: {e}")
            await cl.Message(
                content=f"⚠️ 记忆系统初始化失败: {e}\n\n你仍然可以聊天，但不会保存记忆。"
            ).send()
            return
    
    # 发送欢迎消息到聊天界面
    welcome_message = """
👋 **你好！我是你的本地记忆助手**

我使用以下技术为你服务：
- 🤖 **大模型**: Ollama (gemma3:27b)
- 🧠 **记忆系统**: Mem0 (本地向量数据库)
- 🎨 **界面**: Chainlit

我会记住我们的对话内容，下次你可以问我之前聊过的事情！

试着告诉我一些关于你的信息吧 😊
    """
    
    # 发送欢迎消息到界面
    await cl.Message(content=welcome_message).send()


# ========================================
# Chainlit 事件：接收用户消息
# ========================================
@cl.on_message
async def on_message(message: cl.Message):
    """
    当用户发送消息时触发此函数
    处理消息、检索记忆、调用 Ollama、保存新记忆
    
    参数:
        message (cl.Message): Chainlit 的消息对象，包含用户输入
    """
    # 获取用户输入的文本内容
    user_input = message.content
    
    # ========================================
    # 重要：使用固定的 user_id 来确保记忆持久化
    # ========================================
    # 如果使用 cl.user_session.get("id")，每次重启应用 ID 都会变化
    # 导致无法检索到之前的记忆，所以这里使用固定的用户 ID
    user_id = "default_user"  # 固定的用户 ID，可以根据需要修改
    
    # 可选：如果需要多用户支持，可以使用其他方式获取用户标识
    # 例如：user_id = cl.user_session.get("user_email") or "default_user"
    
    print(f"📝 当前用户 ID: {user_id}")  # 调试输出
    
    # ========================================
    # 第一步：从 Mem0 检索相关记忆
    # ========================================
    relevant_memories = []  # 初始化相关记忆列表
    
    if memory is not None:  # 如果 Mem0 已初始化
        try:
            # 从 Mem0 搜索与当前消息相关的记忆
            # search() 方法会根据语义相似度返回相关记忆
            search_results = memory.search(
                query=user_input,  # 搜索查询（用户的当前消息）
                user_id=user_id,   # 用户 ID（每个用户有独立的记忆空间）
                limit=3            # 最多返回 3 条相关记忆
            )
            
            # 提取记忆内容
            if search_results and 'results' in search_results:
                relevant_memories = [
                    mem['memory'] for mem in search_results['results']
                ]
                
                # 如果找到了相关记忆，打印到控制台（调试用）
                if relevant_memories:
                    print(f"🧠 检索到 {len(relevant_memories)} 条相关记忆:")
                    for i, mem in enumerate(relevant_memories, 1):
                        print(f"   [{i}] {mem}")
                else:
                    print("🧠 未检索到相关记忆（这可能是新对话）")
        
        except Exception as e:
            # 如果检索失败，打印错误（不影响聊天继续）
            print(f"⚠️ 记忆检索失败: {e}")
    
    # ========================================
    # 第二步：构建发送给 Ollama 的消息
    # ========================================
    # 如果有相关记忆，将其添加到系统提示中
    system_message = "你是一个友好的助手。"
    
    if relevant_memories:
        # 将记忆整合到系统消息中
        memory_context = "\n".join([f"- {mem}" for mem in relevant_memories])
        system_message += f"\n\n**你对用户的记忆**:\n{memory_context}\n\n请在回答时自然地运用这些信息。"
    
    # 构建消息列表（Ollama 的 chat API 格式）
    messages = [
        {"role": "system", "content": system_message},  # 系统消息（包含记忆上下文）
        {"role": "user", "content": user_input}         # 用户消息
    ]
    
    # ========================================
    # 第三步：调用 Ollama 生成回复
    # ========================================
    # 显示"正在思考"的提示（Chainlit 会显示加载动画）
    async with cl.Step(name="🤔 正在思考..."):
        response_text = call_ollama(messages)  # 调用 Ollama API
    
    # ========================================
    # 第四步：将新对话保存到 Mem0
    # ========================================
    if memory is not None:
        try:
            # 将用户输入和 AI 回复作为对话保存到 Mem0
            # add() 方法会自动提取关键信息并存储为记忆
            add_result = memory.add(
                messages=[
                    {"role": "user", "content": user_input},
                    {"role": "assistant", "content": response_text}
                ],
                user_id=user_id  # 关联到当前用户
            )
            print(f"💾 对话已保存到记忆")
            print(f"   返回结果: {add_result}")
        
        except Exception as e:
            # 如果保存失败，打印错误（不影响回复显示）
            print(f"⚠️ 记忆保存失败: {e}")
    
    # ========================================
    # 第五步：发送回复到用户界面
    # ========================================
    await cl.Message(content=response_text).send()


# ========================================
# 主程序入口（可选）
# ========================================
if __name__ == "__main__":
    # 提示：不要直接运行此文件
    # 应该使用 Chainlit 命令启动：chainlit run app.py
    print("⚠️ 请使用以下命令启动应用：")
    print("   chainlit run app.py -w")
